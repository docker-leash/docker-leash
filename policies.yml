---

openbar:
  any:
    Allow:

personnal:
  containersCreate:
    ContainerName: "^foo-"

restricted:
  containers:
    BindMounts:
      - "-/"
      - "+/home/$USER"
      - "+/0"
  containersDelete:
    Deny:

readonly:
  systemPing:
    Allow:
  containersList:
    Allow:

...
